# Example assets

Put local images here while experimenting.

Images are ignored by git on purpose. The original research folder contained generated outputs and third-party/watermarked menu images, so they are not committed to the public package.

